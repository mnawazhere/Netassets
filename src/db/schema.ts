// Drizzle schema — populated in Stage 2 (see docs/asset-tracker-build-spec.md §7).
// Tables: assets, lots, transactions, valuation_marks, imports, settings,
// fx_rates, price_cache, change_log. UUID primary keys everywhere.
export {};
